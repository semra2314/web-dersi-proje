# Frontend

Bu klasör proje frontend'ini içerir (React + Vite).

Kurulum

```bash
cd frontend
npm install
```

Geliştirme

```bash
npm run dev
```

Prod

```bash
npm run build
npm run preview
```

Notlar
- Giriş yapıldıktan sonra backend ile iletişim `src/utils/api.ts` üzerinden yapılır.
