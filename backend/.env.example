## Örnek ortam değişkenleri (development)
# Eğer production'da Postgres veya remote DB kullanacaksanız DATABASE_URL girin

# Postgres örneği:
# DATABASE_URL=postgres://user:password@host:5432/dbname

# SQLite için development: db.sqlite kullanılır (varsayılan)

# JWT secret (kod içinde sabit olarak 'your-secret-key' var; production için değiştirin)
JWT_SECRET=your-secret-key
